/**
 * Enumeración HttpResponseCode
 * 
 */

export enum HttpResponse
{
    // Mensajes HTTP
    Ok              = 200,
    
    // Errores HTTP
    BadRequest      = 400,
    Forbidden       = 403,
    NotFound        = 404
}
